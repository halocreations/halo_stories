import 'package:flutter/material.dart';

class StCeciliaStory extends StatelessWidget {
  const StCeciliaStory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'St Cecilia Story'
        )
      ),
      body: Center(
        child: Text(
          'St Cecilia'
        )
      )
    );
  }
}